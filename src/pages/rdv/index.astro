---
import Base from '../../layouts/Base.astro';
import { avocatsRdv } from '../../data/avocats-rdv';

const joursNoms: Record<string, string> = {
  '1': 'lun.', '2': 'mar.', '3': 'mer.', '4': 'jeu.', '5': 'ven.', '6': 'sam.', '0': 'dim.',
};

const avocatsData = avocatsRdv.map(a => ({
  slug:      a.slug,
  titre:     a.titre,
  prenom:    a.prenom,
  specialite: a.specialite,
  photo:     a.photo,
  schedule:  a.schedule,
  jours:     Object.keys(a.schedule).sort().map(k => joursNoms[k]).join(' · '),
}));
---

<Base
  title="Prendre rendez-vous — Custos Avocats"
  description="Réservez un rendez-vous avec l'avocat de votre choix au cabinet Custos Avocats à Roubaix."
>
  <div id="rdv-page">

    <!-- ── Hero ── -->
    <section class="rdv-hero-index" aria-label="Prise de rendez-vous">
      <h1>Prendre rendez-vous</h1>
      <p class="rdv-hero-sub">Choisissez l'avocat avec lequel vous souhaitez consulter, ou continuez sans préférence pour voir toutes les disponibilités.</p>
    </section>

    <!-- ── Choix avocat ── -->
    <section class="rdv-chooser-section" aria-label="Choisir un avocat">
      <div class="rdv-chooser-grid">
        {avocatsData.map(a => (
          <a href={`/rdv/${a.slug}`} class="rdv-card" aria-label={`Choisir ${a.titre}`}>
            <div class="rdv-card-photo">
              <img src={a.photo} alt={a.titre} onerror="this.parentElement.classList.add('rdv-card-photo--empty')" />
            </div>
            <div class="rdv-card-body">
              <span class="rdv-card-specialite">Droit Pénal — {a.specialite}</span>
              <strong class="rdv-card-nom">{a.titre}</strong>
              <span class="rdv-card-jours">{a.jours}</span>
            </div>
          </a>
        ))}
      </div>
    </section>

    <!-- ── Sans préférence ── -->
    <section class="rdv-sanspref-section" aria-label="Toutes les disponibilités">
      <div class="rdv-sanspref-header">
        <div class="rdv-divider-line" aria-hidden="true"></div>
        <span class="rdv-divider-label">ou sans préférence d'avocat</span>
        <div class="rdv-divider-line" aria-hidden="true"></div>
      </div>
      <p class="rdv-sanspref-sub">Votre dossier sera attribué à l'avocat disponible le plus adapté à votre situation.</p>

      <div class="rdv-booking-inner">
        <div class="rdv-col">
          <p class="rdv-step">1 — Choisissez une date</p>
          <div id="cal-container"></div>
        </div>
        <div class="rdv-col">
          <p class="rdv-step">2 — Choisissez un créneau</p>
          <div id="slots-container">
            <p class="rdv-hint">Sélectionnez d'abord une date disponible.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- ── Formulaire ── -->
    <section id="form-section" class="rdv-form-section rdv-hidden" aria-label="Vos coordonnées">
      <div class="rdv-form-wrap">
        <p class="rdv-step">3 — Vos coordonnées</p>
        <div id="rdv-recap" class="rdv-recap" role="status"></div>
        <form id="rdv-form" novalidate autocomplete="off">
          <div class="rdv-two-cols">
            <div>
              <label for="rdv-prenom">Prénom</label>
              <input type="text" id="rdv-prenom" name="prenom" required autocomplete="given-name" />
            </div>
            <div>
              <label for="rdv-nom">Nom</label>
              <input type="text" id="rdv-nom" name="nom" required autocomplete="family-name" />
            </div>
          </div>
          <div>
            <label for="rdv-email">Email</label>
            <input type="email" id="rdv-email" name="email" required autocomplete="email" />
          </div>
          <div>
            <label for="rdv-tel">Téléphone</label>
            <input type="tel" id="rdv-tel" name="tel" autocomplete="tel" />
          </div>
          <div>
            <label for="rdv-motif">Motif (facultatif)</label>
            <textarea id="rdv-motif" name="motif" rows="3" placeholder="Décrivez brièvement votre situation…"></textarea>
          </div>
          <button type="submit">Confirmer le rendez-vous</button>
        </form>
      </div>
    </section>

    <!-- ── Succès ── -->
    <section id="success-section" class="rdv-success rdv-hidden" aria-live="polite">
      <div class="rdv-success-wrap">
        <div class="rdv-check-icon" aria-hidden="true">
          <svg viewBox="0 0 52 52" fill="none">
            <circle cx="26" cy="26" r="25" stroke="currentColor" stroke-width="2"/>
            <path d="M15 26l8 9 14-16" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </div>
        <h2>Rendez-vous confirmé</h2>
        <div id="success-details" class="rdv-success-details"></div>
        <p class="rdv-success-note">Un email de confirmation vous sera envoyé dans les prochaines minutes.</p>
        <a href="/" class="rdv-retour">Retour à l'accueil</a>
      </div>
    </section>

  </div>
</Base>

<style is:global>
  #rdv-page > section {
    max-width: var(--max-width);
    margin-inline: auto;
    padding: 4rem var(--gap);
    border-bottom: 1px solid var(--gris-clair);
  }

  .rdv-hidden { display: none !important; }

  /* ── Hero index ── */
  .rdv-hero-index h1 { margin-bottom: 0.75rem; }
  .rdv-hero-sub {
    font-size: 1rem;
    color: var(--anthracite);
    max-width: 52ch;
    margin: 0 !important;
  }

  /* ── Cards chooser ── */
  .rdv-chooser-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 1rem;
  }

  .rdv-card {
    display: flex;
    flex-direction: column;
    background: var(--blanc);
    border: 1px solid var(--gris-clair);
    border-radius: 1rem;
    text-decoration: none !important;
    color: var(--texte) !important;
    transition: border-color 0.2s, box-shadow 0.2s, transform 0.2s;
    cursor: pointer;
    overflow: hidden;
  }

  .rdv-card:hover {
    border-color: var(--vert);
    box-shadow: 0 6px 24px rgba(74,93,78,0.13);
    background: var(--blanc) !important;
    color: var(--texte) !important;
    transform: translateY(-3px);
  }

  .rdv-card-photo {
    width: 100%;
    aspect-ratio: 3 / 4;
    overflow: hidden;
    background: var(--fond);
    position: relative;
  }

  .rdv-card-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: top;
    display: block;
    transition: transform 0.35s ease;
  }

  .rdv-card:hover .rdv-card-photo img {
    transform: scale(1.04);
  }

  .rdv-card-photo--empty {
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--gris-clair);
  }

  .rdv-card-body {
    padding: 1rem 1rem 1.1rem;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    border-top: 1px solid var(--gris-clair);
  }

  .rdv-card-specialite {
    font-size: 0.63rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--vert);
  }

  .rdv-card-nom {
    font-family: var(--font-serif);
    font-size: 0.9rem;
    color: var(--texte);
    font-weight: 700;
    line-height: 1.3;
  }

  .rdv-card-jours {
    font-size: 0.7rem;
    color: var(--gris);
    margin-top: 0.15rem;
  }

  /* ── Sans préférence separator ── */
  .rdv-sanspref-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 0.75rem;
  }

  .rdv-divider-line {
    flex: 1;
    height: 1px;
    background: var(--gris-clair);
  }

  .rdv-divider-label {
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--gris);
    white-space: nowrap;
  }

  .rdv-sanspref-sub {
    font-size: 0.85rem;
    color: var(--gris);
    margin-bottom: 2rem !important;
    max-width: 52ch;
  }

  /* ── Booking layout ── */
  .rdv-booking-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3.5rem;
    align-items: start;
  }

  @media (max-width: 720px) {
    .rdv-booking-inner { grid-template-columns: 1fr; gap: 2rem; }
  }

  .rdv-step {
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--vert);
    margin-bottom: 1.25rem !important;
    max-width: none !important;
  }

  .rdv-hint {
    font-size: 0.875rem;
    color: var(--gris);
    font-style: italic;
    margin: 0 !important;
  }

  /* ── Calendrier nav ── */
  .rdv-cal-nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 0.85rem;
  }

  .rdv-cal-nav-btn {
    background: none;
    border: 1px solid var(--gris-clair);
    border-radius: var(--radius);
    width: 30px;
    height: 30px;
    cursor: pointer;
    font-size: 1rem;
    color: var(--texte);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: border-color 0.2s, color 0.2s;
    padding: 0;
    line-height: 1;
  }

  .rdv-cal-nav-btn:hover:not(:disabled) { border-color: var(--vert); color: var(--vert); }
  .rdv-cal-nav-btn:disabled { opacity: 0.28; cursor: not-allowed; }

  .rdv-cal-month-label {
    font-family: var(--font-serif);
    font-weight: 700;
    font-size: 0.95rem;
    color: var(--texte);
  }

  .rdv-cal-grid {
    display: grid;
    grid-template-columns: repeat(7, 1fr);
    gap: 2px;
  }

  .rdv-cal-hd {
    font-size: 0.65rem;
    font-weight: 700;
    color: var(--gris);
    text-align: center;
    padding: 0.25rem 0 0.5rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .rdv-cal-day {
    aspect-ratio: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.825rem;
    border-radius: 50%;
    color: var(--gris-clair);
    position: relative;
    transition: background 0.15s, color 0.15s;
    user-select: none;
  }

  .rdv-cal-day.rdv-avail {
    color: var(--texte);
    font-weight: 600;
    cursor: pointer;
  }

  .rdv-cal-day.rdv-avail:hover { background: var(--vert-pale); color: var(--vert); }

  .rdv-cal-day.rdv-today::after {
    content: '';
    position: absolute;
    bottom: 3px;
    left: 50%;
    transform: translateX(-50%);
    width: 3px;
    height: 3px;
    background: var(--vert);
    border-radius: 50%;
  }

  .rdv-cal-day.rdv-selected { background: var(--vert) !important; color: #F4F5F2 !important; }
  .rdv-cal-day.rdv-selected::after { background: #F4F5F2; }

  /* ── Créneaux combinés ── */
  .rdv-slots-date {
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--texte);
    margin-bottom: 0.85rem;
  }

  .rdv-slots-grid { display: flex; flex-wrap: wrap; gap: 0.45rem; }

  .rdv-slot {
    background: none;
    border: 1px solid var(--vert);
    color: var(--vert);
    padding: 0.4rem 0.85rem;
    border-radius: 999px;
    font-family: var(--font-sans);
    font-size: 0.82rem;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.18s, color 0.18s;
    line-height: 1.3;
    text-align: left;
  }

  .rdv-slot:hover { background: var(--vert); color: #F4F5F2; }

  .rdv-slot-avocat {
    display: block;
    font-size: 0.68rem;
    font-weight: 500;
    opacity: 0.8;
    margin-top: 0.1rem;
  }

  .rdv-slot.rdv-slot-selected { background: var(--vert); color: #F4F5F2; }
  .rdv-slot.rdv-slot-selected .rdv-slot-avocat { opacity: 0.9; }

  /* ── Formulaire ── */
  .rdv-form-wrap { max-width: 600px; }

  .rdv-recap {
    background: var(--vert-pale);
    border: 1px solid var(--vert);
    border-radius: var(--radius);
    padding: 0.8rem 1.1rem;
    font-size: 0.875rem;
    color: var(--vert);
    font-weight: 600;
    margin-bottom: 1.75rem;
    max-width: none !important;
  }

  .rdv-two-cols { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }

  @media (max-width: 480px) { .rdv-two-cols { grid-template-columns: 1fr; } }

  #rdv-form { max-width: 100%; margin-top: 0; }

  /* ── Succès ── */
  .rdv-success { text-align: center; }

  .rdv-success-wrap {
    max-width: 460px;
    margin-inline: auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .rdv-check-icon { width: 60px; height: 60px; color: var(--vert); }
  .rdv-check-icon svg { width: 100%; height: 100%; }

  .rdv-success-details {
    background: var(--blanc);
    border: 1px solid var(--gris-clair);
    border-left: 3px solid var(--vert);
    border-radius: var(--radius);
    padding: 1rem 1.25rem;
    text-align: left;
    width: 100%;
    font-size: 0.875rem;
    line-height: 1.9;
    color: var(--anthracite);
    max-width: none !important;
  }

  .rdv-success-note {
    font-size: 0.8rem;
    color: var(--gris);
    margin: 0 !important;
    max-width: 38ch !important;
  }

  .rdv-retour {
    display: inline-block !important;
    background: var(--vert) !important;
    color: #F4F5F2 !important;
    border: none !important;
    padding: 0.65rem 1.5rem !important;
    border-radius: var(--radius) !important;
    font-size: 0.875rem !important;
    font-weight: 600 !important;
    letter-spacing: 0.03em !important;
    text-decoration: none !important;
    margin-top: 0.5rem !important;
    transition: background 0.2s !important;
  }

  .rdv-retour:hover { background: var(--vert-vif) !important; color: #F4F5F2 !important; }

  /* ── Mobile ── */
  @media (max-width: 640px) {
    #rdv-page > section {
      padding: 2.5rem 1rem;
    }

    /* Hero */
    .rdv-hero-sub {
      font-size: 0.9rem;
      max-width: 100%;
    }

    /* Cards avocats */
    .rdv-chooser-grid { grid-template-columns: repeat(2, 1fr); }
    .rdv-card-nom { font-size: 0.82rem; }

    /* Séparateur */
    .rdv-divider-label {
      white-space: normal;
      text-align: center;
    }
    .rdv-sanspref-sub {
      max-width: 100%;
    }

    /* Booking */
    .rdv-booking-inner {
      gap: 1.5rem;
    }

    /* Calendrier */
    .rdv-cal-day {
      font-size: 0.8rem;
    }
    .rdv-cal-hd {
      font-size: 0.6rem;
      letter-spacing: 0;
    }

    /* Créneaux combinés : 2 par ligne (plus large car contient le nom) */
    .rdv-slots-grid {
      gap: 0.4rem;
    }
    .rdv-slot {
      flex: 1;
      min-width: calc(50% - 0.4rem);
      padding: 0.55rem 0.75rem;
      min-height: 52px;
    }

    /* Formulaire */
    .rdv-form-wrap {
      max-width: 100%;
    }
    .rdv-two-cols {
      grid-template-columns: 1fr;
    }
    .rdv-recap {
      font-size: 0.825rem;
      word-break: break-word;
    }

    /* Succès */
    .rdv-success-wrap {
      max-width: 100%;
    }
    .rdv-success-note {
      max-width: 100% !important;
    }
  }
</style>

<script define:vars={{ allAvocats: avocatsData }}>
  const MONTHS    = ['Janvier','Février','Mars','Avril','Mai','Juin','Juillet','Août','Septembre','Octobre','Novembre','Décembre'];
  const MONTH_LONG = ['janvier','février','mars','avril','mai','juin','juillet','août','septembre','octobre','novembre','décembre'];
  const DAY_ABBR  = ['Lu','Ma','Me','Je','Ve','Sa','Di'];
  const DAY_LONG  = ['lundi','mardi','mercredi','jeudi','vendredi','samedi','dimanche'];

  const today = new Date();
  today.setHours(0, 0, 0, 0);

  let viewYear  = today.getFullYear();
  let viewMonth = today.getMonth();
  let selDate   = null;
  let selSlot   = null;
  let selAvocat = null; // { titre, slug }

  function isBooked(dateStr, slot) {
    const s = dateStr + '|' + slot;
    let h = 5381;
    for (let i = 0; i < s.length; i++) h = ((h << 5) + h) ^ s.charCodeAt(i);
    return (Math.abs(h) % 10) < 3;
  }

  function toDateStr(date) {
    return `${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}-${String(date.getDate()).padStart(2,'0')}`;
  }

  function formatDateLong(ds) {
    const [y, m, d] = ds.split('-').map(Number);
    const date = new Date(y, m - 1, d);
    const dow = (date.getDay() + 6) % 7;
    return `${DAY_LONG[dow]} ${d} ${MONTH_LONG[m - 1]} ${y}`;
  }

  // Retourne les créneaux libres de tous les avocats pour une date donnée
  function getCombinedSlots(date) {
    const ds  = toDateStr(date);
    const dow = String(date.getDay());
    const result = [];

    allAvocats.forEach(av => {
      const slots = av.schedule[dow] || [];
      slots.forEach(slot => {
        if (!isBooked(ds, slot)) {
          result.push({ slot, titre: av.titre, slug: av.slug, prenom: av.prenom });
        }
      });
    });

    result.sort((a, b) => a.slot.localeCompare(b.slot));
    return result;
  }

  function hasAvailOnDay(date) {
    if (date < today) return false;
    return getCombinedSlots(date).length > 0;
  }

  // ── Calendrier ──
  function renderCal() {
    const firstDay    = new Date(viewYear, viewMonth, 1);
    const daysInMonth = new Date(viewYear, viewMonth + 1, 0).getDate();
    const offset      = (firstDay.getDay() + 6) % 7;
    const isPrevDisabled = viewYear === today.getFullYear() && viewMonth === today.getMonth();

    let html = `
      <div class="rdv-cal-nav">
        <button class="rdv-cal-nav-btn" id="rdv-prev" ${isPrevDisabled ? 'disabled' : ''} aria-label="Mois précédent">&#8249;</button>
        <span class="rdv-cal-month-label">${MONTHS[viewMonth]} ${viewYear}</span>
        <button class="rdv-cal-nav-btn" id="rdv-next" aria-label="Mois suivant">&#8250;</button>
      </div>
      <div class="rdv-cal-grid">
    `;

    DAY_ABBR.forEach(d => { html += `<div class="rdv-cal-hd">${d}</div>`; });
    for (let i = 0; i < offset; i++) html += '<div></div>';

    for (let d = 1; d <= daysInMonth; d++) {
      const date  = new Date(viewYear, viewMonth, d);
      const ds    = toDateStr(date);
      const avail = hasAvailOnDay(date);
      const isToday = date.toDateString() === today.toDateString();
      const isSel   = ds === selDate;

      let cls = 'rdv-cal-day';
      if (avail)   cls += ' rdv-avail';
      if (isToday) cls += ' rdv-today';
      if (isSel)   cls += ' rdv-selected';

      html += `<div class="${cls}" ${avail ? `data-date="${ds}" tabindex="0" role="button" aria-label="${formatDateLong(ds)}"` : ''}>${d}</div>`;
    }

    html += '</div>';
    document.getElementById('cal-container').innerHTML = html;
  }

  // ── Créneaux ──
  function renderSlots() {
    const container = document.getElementById('slots-container');
    if (!selDate) {
      container.innerHTML = '<p class="rdv-hint">Sélectionnez d\'abord une date disponible.</p>';
      return;
    }

    const [y, m, d] = selDate.split('-').map(Number);
    const slots = getCombinedSlots(new Date(y, m - 1, d));

    let html = `<p class="rdv-slots-date">${formatDateLong(selDate)}</p><div class="rdv-slots-grid">`;
    slots.forEach(s => {
      const isSel = selSlot === s.slot && selAvocat && selAvocat.slug === s.slug;
      const cls = 'rdv-slot' + (isSel ? ' rdv-slot-selected' : '');
      html += `<button class="${cls}" data-slot="${s.slot}" data-titre="${s.titre}" data-slug="${s.slug}">
        ${s.slot}<span class="rdv-slot-avocat">M<sup>e</sup> ${s.prenom}</span>
      </button>`;
    });
    html += '</div>';
    container.innerHTML = html;
  }

  // ── Formulaire ──
  function showForm() {
    const section = document.getElementById('form-section');
    section.classList.remove('rdv-hidden');
    document.getElementById('rdv-recap').textContent =
      `${selAvocat.titre} — ${formatDateLong(selDate)} à ${selSlot}`;
    section.scrollIntoView({ behavior: 'smooth', block: 'start' });
  }

  function hideForm() {
    document.getElementById('form-section').classList.add('rdv-hidden');
  }

  // ── Succès ──
  function showSuccess(prenom, nom, email) {
    hideForm();
    document.querySelector('.rdv-sanspref-section').classList.add('rdv-hidden');
    const section = document.getElementById('success-section');
    section.classList.remove('rdv-hidden');
    document.getElementById('success-details').innerHTML =
      `<strong>Avocat :</strong> ${selAvocat.titre}<br>` +
      `<strong>Date :</strong> ${formatDateLong(selDate)}<br>` +
      `<strong>Heure :</strong> ${selSlot}<br>` +
      `<strong>Client :</strong> ${prenom} ${nom.toUpperCase()}<br>` +
      `<strong>Email :</strong> ${email}`;
    section.scrollIntoView({ behavior: 'smooth', block: 'start' });
  }

  // ── Événements ──
  document.getElementById('cal-container').addEventListener('click', e => {
    const prev = e.target.closest('#rdv-prev');
    const next = e.target.closest('#rdv-next');
    const day  = e.target.closest('.rdv-avail');

    if (prev && !prev.disabled) {
      viewMonth--;
      if (viewMonth < 0) { viewMonth = 11; viewYear--; }
      renderCal();
      return;
    }
    if (next) {
      viewMonth++;
      if (viewMonth > 11) { viewMonth = 0; viewYear++; }
      renderCal();
      return;
    }
    if (day) {
      selDate   = day.dataset.date;
      selSlot   = null;
      selAvocat = null;
      renderCal();
      renderSlots();
      hideForm();
    }
  });

  document.getElementById('cal-container').addEventListener('keydown', e => {
    if ((e.key === 'Enter' || e.key === ' ') && e.target.classList.contains('rdv-avail')) {
      e.preventDefault();
      e.target.click();
    }
  });

  document.getElementById('slots-container').addEventListener('click', e => {
    const btn = e.target.closest('.rdv-slot');
    if (btn) {
      selSlot   = btn.dataset.slot;
      selAvocat = { titre: btn.dataset.titre, slug: btn.dataset.slug };
      renderSlots();
      showForm();
    }
  });

  document.getElementById('rdv-form').addEventListener('submit', e => {
    e.preventDefault();
    const prenom = document.getElementById('rdv-prenom').value.trim();
    const nom    = document.getElementById('rdv-nom').value.trim();
    const email  = document.getElementById('rdv-email').value.trim();
    if (!prenom || !nom || !email) return;
    showSuccess(prenom, nom, email);
  });

  // ── Init ──
  renderCal();
</script>
